# Copy to `.env` and fill in values. python-dotenv will load it.

# Required if you want to use OpenAI-backed LLMs (QueryRunner or HP-RAG TOC selection)
 
# Optional: default model name used when none is provided in code
# Examples: gpt-4o-mini, gpt-4o
LLM_MODEL=gpt-4o-mini

# Optional: keep DeepEval fully offline
DEEPEVAL_TELEMETRY_OPTOUT=1
